repos:
  - repo: local
    hooks:
      - id: pytest-wedding-guest-network
        name: pytest (wedding-guest-network)
        entry: python -m pytest apps/wedding-guest-network/tests/ -q --tb=short
        language: system
        pass_filenames: false
        always_run: true
